# Единый бот курьеров

Telegram-бот для управления курьерами на **Node.js + grammY + PostgreSQL**.

Текущий этап — **Раздел 1: каркас + регистрация** (согласие на обработку ПД,
присвоение системного ID, регистрация нового/действующего курьера, городские
поля Москвы и СПб, отправка заявки менеджеру, просмотр/изменение/сброс данных).

## Установка

```bash
npm install
cp .env.example .env      # заполните BOT_TOKEN и DATABASE_URL
npm run migrate           # создаст таблицы в PostgreSQL
npm start                 # запуск бота (long polling)
```

Для разработки: `npm run dev` (авто-перезапуск при изменениях).

## Переменные окружения (.env)

| Переменная        | Назначение                                              |
|-------------------|---------------------------------------------------------|
| `BOT_TOKEN`       | Токен бота от @BotFather (обязательно)                  |
| `DATABASE_URL`    | Строка подключения к PostgreSQL (обязательно)           |
| `MANAGER_CHAT_ID` | Чат менеджеров для заявок на регистрацию (опционально)  |
| `WEBAPP_URL`      | Ссылка на Telegram Mini-App (опционально)               |
| `KIS_ART_LINK`    | Ссылка для создания КИС АРТ, Москва (опционально)        |

## Структура

```
src/
  index.js            — точка входа, запуск polling
  config.js           — конфиг из .env
  db/
    pool.js           — пул PostgreSQL
    schema.sql        — схема таблиц
    migrate.js        — применение схемы (npm run migrate)
  services/
    courier.js        — доступ к данным курьеров
  bot/
    index.js          — сборка бота, маршрутизация
    fields.js         — определения полей анкеты (1.4 / 1.5)
    flow.js           — пошаговый движок регистрации
    keyboards.js      — клавиатуры
    menu.js           — главное меню (раздел 2) + «Мои данные»
    notify.js         — уведомление менеджеру
```

## Реализовано (раздел 1)

- [x] 1.2 Согласие на обработку персональных данных
- [x] 1.2.1 Первый вход — присвоение уникального системного ID
- [x] 1.3 Выбор: новый / действующий курьер
- [x] 1.4 Регистрация нового курьера + городские поля (Москва / СПб)
- [x] 1.5 Действующий курьер + изменение / сброс данных
- [x] Отправка заявки в чат менеджера

## Дорожная карта (следующие этапы)

- [ ] 1.1 Telegram Mini-App
- [ ] 1.2.1 Подтягивание данных по ID из базы менеджера
- [ ] 1.6 ИИ-клиент
- [ ] 2.2 Отчёт слоты (выход / невыход / ранний сход / завершение смены)
- [ ] 2.3 Связь с менеджером
- [ ] 2.4 Слоты (биржа, отдать / запросить слот)
- [ ] 2.5 Светофор, экипировка, претензии
- [ ] 2.6 Информация по проекту
- [ ] 2.7 Отчёты (штрафы, доход, заказы)
- [ ] 2.8 Пуш-уведомления (напоминание о слоте + бот-звонарь)
- [ ] 2.9 Рабочие приложения
- [ ] Интеграции (Google Sheets / CRM) и админка
```
